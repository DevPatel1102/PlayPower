// Note: In JavaScript, we don't have TypeScript interfaces
// These are just documentation comments for the expected data structures

// NoteId is a string
// NoteVersion: { versionId: string, createdAt: number, title: string, html: string }
// Note: { id: string, createdAt: number, updatedAt: number, title: string, html: string, pinned: boolean, tags: string[], encrypted: boolean, iv?: string, salt?: string, versions: NoteVersion[] }
// AiInsights: { summary: string, tags: string[], glossary: Array<{ term: string, definition: string }>, translations?: Record<string, string> }
